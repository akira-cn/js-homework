num = prompt("Input a string:");

switch (parseInt(num) == num) {
    case false:
        alert("Is not an integer!");
        break;
    default:
        a = parseInt(num);
        if (a % 2 == 0) {
            alert("even");
        } else {
            alert("odd");
        }
        break;
}
